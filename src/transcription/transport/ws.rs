//! Shared WebSocket transport helpers.
//!
//! Populated in a later step — currently empty so the module path
//! resolves.  The `realtime.rs` / `openai_realtime.rs` WS-specific
//! helpers (request builder, ping keepalive) will move here.
